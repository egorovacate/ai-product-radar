# AI Product Radar

When asked to collect news or create a digest, use the `ai-product-radar` skill in `codex-skill/ai-product-radar`.

Never overwrite an existing digest. Do not commit, push, deploy, or send notifications unless explicitly requested. Preserve primary-source links and distinguish source claims from curator inference.

